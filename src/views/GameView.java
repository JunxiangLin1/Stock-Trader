package views;

/**
 * The view that handles displaying outputs to the user.
 */
public interface GameView {

  /**
   * Displays the given message to the user.
   *
   * @param message the message to display
   */
  public void displayMessage(String message);
}
